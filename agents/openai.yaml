display_name: AI Coding Safety Gate
short_description: Discuss before edits; check hardware and symptoms before debugging changes
default_prompt: In any AI coding, debugging, or test-failure task, first enforce the safety gate: discuss code changes before editing, require explicit approval, and ask for hardware checks and concrete symptoms before modifying code for waveform, data, jitter, unstable, crash, or hang issues.
